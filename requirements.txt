# Python: use 3.10–3.13. langchain-pinecone (and peers) declare Requires-Python <3.14, so 3.14+ cannot install yet.
# Core LangChain (use latest stable, test compatibility)
langchain>=0.1.0
langchain-openai>=0.0.5
langchain-community>=0.0.20
langgraph>=0.0.20
langchain-pinecone>=0.2.0
langchain-text-splitters>=0.3.2
pinecone-client>=6.0.0

# Log Parsing & Routing Upgrades
pygrok>=1.0.0
semantic-router>=0.1.12

# LLM Providers
openai>=1.0.0
huggingface-hub>=0.19.0

fastapi>=0.115.0
uvicorn>=0.30.0

# Utilities
python-dotenv>=1.0.0
requests>=2.31.0
OTXv2>=1.5.12
beautifulsoup4>=4.12.0
pandas>=2.1.0
matplotlib>=3.8.0
numpy>=1.26.0

# Embedding support
# ollama is only needed for local runs with EMBEDDING_PROVIDER=ollama
# It uses urllib (stdlib) to call the Ollama HTTP API — no extra package needed.
# If you prefer the official SDK, uncomment:
# ollama>=0.1.0

# Testing
pytest>=7.4.0
pytest-mock>=3.12.0